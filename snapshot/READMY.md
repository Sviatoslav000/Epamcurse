# Snapshot

A simple Python system monitoring tool based on the `psutil` module.

The tool collects information about:

- Running processes
- CPU usage
- Memory usage
- Swap usage
- Current timestamp

Snapshots are printed to the console and saved to a JSON file.

## Installation

Clone the repository:

```bash
git clone <repository-url>
cd snapshot
